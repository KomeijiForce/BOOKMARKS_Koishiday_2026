[project]
name = "bookmarks-framework"
version = "0.1.0"
description = "BOOKMARKS memory framework benchmark runner"
requires-python = ">=3.10"
dependencies = [
  "openai>=1.0.0",
  "numpy>=1.24.0",
  "tqdm>=4.66.0",
]

[tool.setuptools.packages.find]
where = ["src"]
